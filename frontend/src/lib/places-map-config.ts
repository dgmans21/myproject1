/** 지도 화면: 현재 지도 중심 기준 등록 맛집 반경 필터 (km) */
export const PLACES_MAP_RADIUS_KM = 2.5;

/** 지도 검색 POI 선택 후 확대 레벨 (카카오맵 level, 숫자 클수록 넓게) */
export const PLACES_MAP_SEARCH_ZOOM_LEVEL = 4;

/** 검색으로 지도 중심을 옮길 때 onCenterChanged 디바운스 이벤트 무시 시간(ms) */
export const PLACES_MAP_PROGRAMMATIC_CENTER_SUPPRESS_MS = 500;
